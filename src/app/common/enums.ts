export enum EnumStatus {
  ALL,
  DRAFT,
  SUBMITTED,
  PENDING,
  RETURNED,
  REJECTED,
  APPROVED,
  CONSULT
}

export enum EnumRequestType {
  New = 1,
  Success = 2
}

export enum EnumRole {
  APO,
  DOP_L1,
  DOP_L2,
  DOP_L3
}